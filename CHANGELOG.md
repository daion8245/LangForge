# 변경 이력 — LangForge

## 1.0.0

첫 정식 릴리스입니다. MVP(Phase 0~7) 위에 Phase 8~11 을 얹고 Phase 12 에서 마감했습니다.

### 번역 엔진

- 엔진 4종 지원 — Google Gemini · DeepL · Google Cloud Translation · Papago.
- 엔진마다 인증 필드가 다르게 렌더링되고, `연결 테스트` 가 실제 API 호출로 키를 검증합니다.
- 엔드포인트·모델 목록을 `assets/data/providers.json` 으로 외부화했습니다. 벤더가 주소를
  바꿔도 앱을 다시 빌드하지 않습니다.
- 엔진을 바꿔도 토큰 보호·검증·병합·출력 로직은 그대로입니다.

### 캐시와 용어집

- 같은 원문·같은 조건이면 API 를 호출하지 않습니다. 캐시 키는 8요소로 구성됩니다.
- 캐시를 자동 · 검수 완료 · 사용자 수정으로 구분해 보관합니다.
- 적중률을 화면에 표시합니다. 대상이 0건이면 `NaN%` 가 아니라 `—` 입니다.
- 전역 · 프로젝트 용어집을 등록하고 번역기 호출 **전에** 적용합니다.

### 충돌 해결과 환경설정

- 같은 namespace 의 같은 key 에 원문이 다르면 충돌로 감지하고, S11 에서 원문을 나란히
  비교해 최종 값을 고를 수 있습니다.
- 충돌 우선순위 5단계를 설정할 수 있습니다. `manual` 외 네 단계는 **미리 선택만** 하며,
  사용자가 확인하기 전에는 어떤 것도 확정되지 않습니다.
- 미해결 충돌이 남아 있으면 출력이 차단됩니다.
- 환경설정 4개 탭 — 일반 · 변수 보호 · 충돌 처리 · 언어 프로필.
- 앱 내 로그 뷰어를 추가했습니다.

### 출력과 언어

- 대상 언어를 6종 중에서 고를 수 있습니다. 출력 파일명과 `pack.mcmeta` 설명이 함께 바뀝니다.
- 출력 형식에 **모드별 개별 리소스팩** 을 추가했습니다.
- `pack.png` 를 기본 아이콘 · 사용자 이미지 · 원본 모드 아이콘 · 없음 중에서 고를 수 있습니다.
  사용자 이미지는 PNG · 정사각형 · 크기 · 손상 여부를 검사하고, 모드 아이콘 추출에 실패하면
  기본 아이콘으로 폴백합니다.
- `Translation_Report.md` 가 통계 · 오류 · 경고 · 충돌 · 원문 유지 항목 · 출력 파일 목록을
  모두 담습니다. 출력 파일 표는 실제로 쓰인 파일의 크기를 기록합니다.

### 이번 릴리스의 결정

- **골든(스크린샷) 테스트를 도입하지 않습니다.** 근거는 `docs/TECHNICAL.md` 11.7.
- **코드 서명 인증서를 도입하지 않습니다.** 서명 없는 포터블 ZIP 을 배포하며 SmartScreen
  경고 안내는 README 에 유지합니다.

### 데이터 호환성

- 프로젝트 스키마는 변경되지 않았습니다. MVP 에서 만든 `.lfproj` 를 그대로 엽니다.
- 상위 호환은 보장하지 않습니다. 더 새 버전으로 만든 프로젝트는 구버전에서 열 수 없습니다.

### 알려진 제약

- Windows 데스크톱 전용입니다. macOS · Linux · Android 는 이후 Phase 입니다.
- 자리표시자 보존율 실측(`TECHNICAL.md` Q1)은 엔진별로 아직 채워지지 않았습니다. 보존에
  실패한 값은 멀티셋 검증에서 걸러지므로 게임에는 나가지 않습니다.
